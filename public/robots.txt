# https://www.robotstxt.org/robotstxt.html
User-agent: *
Allow: /

# Admin area — never crawl
Disallow: /admin

# Note: user/utility pages (cart, login, signup, orders, profile, search,
# password reset, email verification) are intentionally crawlable but carry a
# `noindex` meta tag. That lets Google read the tag and drop them from results,
# instead of indexing them URL-only and surfacing them as sitelinks.

Sitemap: https://protacc.in/sitemap.xml
